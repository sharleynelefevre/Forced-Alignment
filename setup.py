from setuptools import setup, find_packages

setup(name='forced_alignment',
      version='1.0',
      packages=find_packages(),
     # packages=['convert','forced-alignment'],
      author='Paul Lerner',
      author_email='1.paul.lerner@gmail.com',
      )
